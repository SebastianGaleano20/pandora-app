import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: '#eb5e28',
    },
    secondary: {
      main: '#403d39',
      light: '#fffcf2',
      dark:'#252422' ,
      contrastText: 'ccc5b9',
    },
  },
});
